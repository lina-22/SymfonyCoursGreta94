<form method="post" action="{{ path('app_delegue_delete', {'id': delegue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delegue.id) }}">
    <button class="btn">Delete</button>
</form>
