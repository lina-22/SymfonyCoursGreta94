{% extends 'base.html.twig' %}

{% block title %}RapportVisiteMedicament index{% endblock %}

{% block body %}
    <h1>RapportVisiteMedicament index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rapport_visite_medicament in rapport_visite_medicaments %}
            <tr>
                <td>{{ rapport_visite_medicament.id }}</td>
                <td>{{ rapport_visite_medicament.nombre }}</td>
                <td>
                    <a href="{{ path('app_rapport_visite_medicament_show', {'id': rapport_visite_medicament.id}) }}">show</a>
                    <a href="{{ path('app_rapport_visite_medicament_edit', {'id': rapport_visite_medicament.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rapport_visite_medicament_new') }}">Create new</a>
{% endblock %}
