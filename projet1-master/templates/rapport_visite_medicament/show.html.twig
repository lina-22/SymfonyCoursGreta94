{% extends 'base.html.twig' %}

{% block title %}RapportVisiteMedicament{% endblock %}

{% block body %}
    <h1>RapportVisiteMedicament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rapport_visite_medicament.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ rapport_visite_medicament.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rapport_visite_medicament_index') }}">back to list</a>

    <a href="{{ path('app_rapport_visite_medicament_edit', {'id': rapport_visite_medicament.id}) }}">edit</a>

    {{ include('rapport_visite_medicament/_delete_form.html.twig') }}
{% endblock %}
