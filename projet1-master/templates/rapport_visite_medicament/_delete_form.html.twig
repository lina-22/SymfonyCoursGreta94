<form method="post" action="{{ path('app_rapport_visite_medicament_delete', {'id': rapport_visite_medicament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_visite_medicament.id) }}">
    <button class="btn">Delete</button>
</form>
